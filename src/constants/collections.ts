// Firestore collection names - centralized to avoid magic strings
export const COLLECTIONS = {
  USERS: 'users',
  PROJECTS: 'projects',
  CASHFLOWS: 'cashflows',
} as const;

// Admin email for bootstrapping
export const ADMIN_EMAIL = 'rkashyapa@qualitastech.com';
